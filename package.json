{
  "name": "node-facebook-marketplace",
  "version": "1.0.0",
  "description": "auto publish marketplace items",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch src --exec node --no-warnings=ExperimentalWarning --loader ts-node/esm server.ts"
  },
  "author": "Eddy",
  "license": "ISC",
  "dependencies": {
    "edge-paths": "^3.0.5",
    "nodemon": "^3.1.0",
    "puppeteer-core": "^22.6.1"
  },
  "type": "module",
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
